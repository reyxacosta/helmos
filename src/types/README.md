# types

Shared TypeScript types and interfaces used across more than one feature or layer (e.g. domain entities, API contracts). Types local to a single feature should stay colocated with that feature instead.
