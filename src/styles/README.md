# styles

Design tokens and theme-level styling (color scales, spacing scale, Tailwind theme extensions) shared across the app. `src/app/globals.css` remains the required Next.js global stylesheet entry point and stays in `app/`; this folder is for the token/theme source it will eventually import from, once a design system is introduced.
